export const enum AppRouteEnum {
  MAIN = '/',
  BUSINESS = '/business',
  BUSINESS_DETAILS = '/business/:id',
  NOT_FOUND = '*',
}

export enum LayoutEnum {
  DEFAULT = 'default',
  AUTH = 'auth',
}
